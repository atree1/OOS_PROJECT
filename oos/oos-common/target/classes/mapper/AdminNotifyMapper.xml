<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.oos.mapper.AdminNotifyMapper">
  
  <select id="getList" parameterType="map" resultType="org.oos.domain.AdminNotifyVO">
  select * from tbl_admin_notify
  where bno > 0 and sid = #{sid} and del = 'n'
  order by bno desc
  limit #{dto.cri.skip}, #{dto.cri.amount}
  </select>
  
  <delete id="delete">
  update tbl_admin_notify
  set del='y'
  where bno = #{bno}
  </delete>
  
  <update id="modify">
  update tbl_admin_notify
  set content = #{content}, title = #{title}, regdate=now(), updatedate = now() 
  where bno = #{bno}	
  </update>
  
  <insert id="insert">
  insert into tbl_admin_notify(title, content, sid)
		values(#{title},#{content},#{sid})
  </insert>
  
  <select id="get" resultType="org.oos.domain.AdminNotifyVO">
  		select * from tbl_admin_notify
		where bno = #{bno}
  </select>
  
  <select id="sidCount" resultType="int">
  		select count(*) from tbl_admin_notify
		where bno > 0 and sid = #{sid} and del = 'N'
  </select>
  
  <select id="count" resultType="int">
		select count(*) from tbl_admin_notify
		where sno > 0 and sno = #{sno} and del='N'
</select>	
  
  </mapper>