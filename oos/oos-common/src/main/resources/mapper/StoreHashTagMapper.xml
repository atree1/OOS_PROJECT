<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.oos.mapper.StoreHashTagMapper">

	<insert id="insert">
	insert into tbl_store_hashTag(sno,hno)
	values (#{sno},#{hno})

	</insert>
	
	<select id="getList">
	select *
	from tbl_store_hashTag
	where shno>0 and sno=#{sno};
	
	</select>
	
</mapper>